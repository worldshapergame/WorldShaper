# 15 — Releases, versions and the "is this a virus" problem

## The version number lives in one place

`CMakeLists.txt`, on the `project()` line. Everything reads it from there:

| Where it shows up | How it gets there |
|---|---|
| The window title — `WorldShaper v0.5.0` | `src/core/version.hpp`, generated by CMake |
| The executable's Details tab in Windows | `src/app/WorldShaper.rc.in`, generated by CMake |
| The startup log line | the same generated header |
| The release tag and the zip's name | the workflow reads `CMakeLists.txt` and **fails if the tag disagrees** |
| What the game compares against to decide it is out of date | the same generated header |

So releasing is: bump the number, commit, tag `v<the same number>`, push the tag.

```bash
git tag v0.6.0 && git push origin v0.6.0
```

The workflow refuses to build a tag that does not match the source. That check is the whole
point: the updater compares the latest release's tag against the version compiled into the
running game, and that comparison is only meaningful if the two can never drift.

## The update check

`src/app/updater.cpp`. On start, on its own thread, it asks GitHub for the latest release,
compares tags, and if there is a newer one says so at the top of the screen. **F8** downloads
and installs it; nothing else does, and nothing happens on its own.

Deliberate choices, each of which could have gone the lazy way:

- **It asks.** An update that installs itself while you were trying to play is not a feature.
- **It only looks at one place.** The URL is built from the owner and repository compiled
  into the build, never from anything the server said, and the download URL is checked
  against the exact prefix a release asset of this repository has before it is fetched. A
  hostile or broken reply cannot redirect the download.
- **It uses the operating system's HTTPS stack** (WinHTTP). No HTTP library to depend on, to
  licence, or to keep patched — which matters for a project that must stay free to publish
  (documentation/08).
- **It never overwrites the running program.** Windows would not allow it anyway. The running
  executable is renamed aside, the new one put in its place, and the old one swept up on the
  next start.
- **It is off in debug builds**, and `--no-update-check` turns it off anywhere.

A failure — no network, GitHub unreachable, a reply that makes no sense — is silent. The
answer is "no update" and the game carries on.

## Why Windows says the game might be dangerous, and what actually helps

Downloading an unsigned executable gets a SmartScreen warning. It is worth being precise
about what that warning means, because the fix people reach for first does not work.

**The warning means "no certificate authority has vouched for this publisher".** It is not a
scan result and it is not a claim that anything is wrong with the file.

### What would remove it

A **code-signing certificate**, bought from a certificate authority, which I cannot obtain
for you — it requires verifying your identity, and it costs money:

- An **OV** (organisation validation) certificate is the cheaper option, and it does *not*
  clear SmartScreen straight away. Reputation accrues per certificate as downloads happen
  without incident, over weeks.
- An **EV** (extended validation) certificate clears SmartScreen immediately, costs more,
  and the key usually arrives on a hardware token or in a cloud signing service.

The release workflow is already wired for it. Add two repository secrets and signing starts
happening on the next release, with no further changes:

| Secret | What goes in it |
|---|---|
| `WS_SIGNING_CERT` | the `.pfx`, base64-encoded |
| `WS_SIGNING_PASSWORD` | its password |

Until those exist, that step is skipped and everything else still runs.

### What is in place already, and is free

None of this removes the warning. All of it is real, and it is what an interested person can
actually check:

- **Build provenance attestation.** Every release carries a statement, signed by GitHub, that
  this exact file came out of this exact commit through this exact workflow. Anyone can
  verify it: `gh attestation verify <the zip> --repo worldshapergame/WorldShaper`. This is
  the closest free equivalent to "prove where it came from", and it is stronger than a
  signature in one respect: a signature says who built it, an attestation says *what source*
  it was built from.
- **A SHA-256 beside the download**, so a file can be checked against what was published.
- **Version information in the executable** — publisher, description, version, project URL.
  A file that says what it is looks like software. A file with every field blank looks like
  something that would rather not say, and heuristics treat it accordingly.
- **An icon.** Not security, but a blank default icon reads as "thrown together".
- **The source is public**, and the workflow that built the file is in it.

### What does not help, and is not done

Uploading to VirusTotal and linking the result. Fresh unsigned executables routinely get a
handful of heuristic hits from smaller engines, and a link showing "3/70" persuades nobody of
anything. If a false positive shows up, the fix is to report it to that vendor.

## The icon

`assets/icon.png` is the source. `tools/make-icon.ps1` builds `assets/icon.ico` from it, at
every size Windows asks for, and CMake compiles that into the executable. Change the logo by
replacing the PNG and running the script.

One thing to know: **the logo is white on transparent**, so it is close to invisible against
a light background — a white Explorer window, or a light-themed taskbar. If that matters,
the icon wants either a dark plate behind it or a dark variant, which is a change to the PNG
rather than to any of this.

`run.bat` cannot have an icon. Batch files always show the Windows batch-file icon; only
`.exe` and `.lnk` carry one. That is why the release ships the executable to be run directly
and does not need `run.bat` at all — `run.bat` is a development convenience that builds first
and then runs.
