# entablature - architrave, frieze and dentilled cornice around the whole building
#
# PLACEHOLDER. Replace everything below with the real part.
# Read clips/facility/BRIEF.md first, then _contract.clip for every dimension.
#
# This file must define exactly one part named `part_entablature`. Every other name it binds must
# start with `entablature_` so nothing collides with another fragment.

let entablature_placeholder = sphere 0 -1.30 0 r=0.25
let part_entablature = entablature_placeholder
