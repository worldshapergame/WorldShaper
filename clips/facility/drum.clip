# drum - the drum of the dome, its pilasters and windows
#
# PLACEHOLDER. Replace everything below with the real part.
# Read clips/facility/BRIEF.md first, then _contract.clip for every dimension.
#
# This file must define exactly one part named `part_drum`. Every other name it binds must
# start with `drum_` so nothing collides with another fragment.

let drum_placeholder = sphere 0 -1.30 0 r=0.25
let part_drum = drum_placeholder
