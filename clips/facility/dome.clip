# dome - the dome, its ribs, oculus and lantern
#
# PLACEHOLDER. Replace everything below with the real part.
# Read clips/facility/BRIEF.md first, then _contract.clip for every dimension.
#
# This file must define exactly one part named `part_dome`. Every other name it binds must
# start with `dome_` so nothing collides with another fragment.

let dome_placeholder = sphere 0 -1.30 0 r=0.25
let part_dome = dome_placeholder
